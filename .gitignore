.DS_Store
assets/files.zip
/wip/

# Scratch/reference material not needed by the shipped app (design handoff
# package, raw source screenshots, Affinity Photo originals) — kept locally
# for reference, never committed. See PROJECT.md's Files table.
/Design Handoff/
/Film Theme Score Tracker.zip
assets/Originals/
assets/Image-Template.af

# Stray leftover file from an earlier session (harmless fragment of app.js);
# sandbox can't unlink it due to the iCloud EPERM issue -- Paul, please
# delete Projects/ScoreCard/q via Finder when convenient.
/q
